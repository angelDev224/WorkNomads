[pytest]
asyncio_mode = auto
testpaths = tests
env =
    DATABASE_URL=sqlite+aiosqlite:///./test_submission.db
    JWT_SECRET_KEY=test-secret-key-for-local-dev
    RABBITMQ_URL=amqp://guest:guest@localhost:5672/
    MINIO_ENDPOINT=localhost:9000
    APP_ENV=test
