# Test dependencies — no compiled C/Rust extensions.
# Works on any Python version (including 3.14) without MSVC or Build Tools.
# Uses aiosqlite (pure Python) instead of asyncpg for the test database.
# miniopy-async is excluded — not used in unit tests.
#
# Install: pip install -r requirements-test.txt
# Run:     pytest -v

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.35
aiosqlite>=0.20.0
pydantic[email]>=2.11
pydantic-settings>=2.5.0
python-jose[cryptography]==3.3.0
httpx>=0.27.0
structlog>=24.0.0
prometheus-fastapi-instrumentator>=7.0.0
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-env==1.1.3
