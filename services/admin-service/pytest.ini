[pytest]
asyncio_mode = auto
testpaths = tests
env =
    DATABASE_URL=sqlite+aiosqlite:///./test_admin.db
    JWT_SECRET_KEY=test-secret-key-for-local-dev
    MINIO_ENDPOINT=localhost:9000
    APP_ENV=test
